Returns one merchant by ID. Requires the `merchantApiKey` security scheme — pass the fixed sample key
as `X-API-Key: sample-api-key-123`. Accepts an optional `env` query parameter (`live` or `sandbox`) to switch
between example datasets.
